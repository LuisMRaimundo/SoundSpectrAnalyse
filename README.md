# SoundSpectralAnalyser — Windows installer

**Scripts for GitHub:** `Windows installer\` only.

Build artefacts (`build\`, `dist\`, `output\`) are created next to those scripts when you run `Build-All.ps1` and are listed in `.gitignore`.

See **`Windows installer\README.md`** for instructions.
